{
  "name": "paysat-login",
  "version": "1.0.0",
  "description": "PaySat — Mobile Login Prototype",
  "private": true,
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
